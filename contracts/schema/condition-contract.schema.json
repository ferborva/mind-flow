{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mind-flow.org/contracts/condition-definition/2-0-0",
  "title": "Seldon Observatory condition definition",
  "description": "An immutable, scoped IF definition. Observations and evaluation runs are separate append-only records.",
  "type": "object",
  "required": [
    "schema_version",
    "id",
    "definition_version",
    "created_at",
    "title",
    "summary",
    "provenance",
    "scope",
    "evidence_requirements",
    "predicates",
    "gates",
    "governance"
  ],
  "properties": {
    "schema_version": { "type": "string", "pattern": "^2\\.0\\.[0-9]+$" },
    "id": { "$ref": "#/$defs/id" },
    "definition_version": { "type": "string", "pattern": "^[1-9][0-9]*\\.[0-9]+\\.[0-9]+$" },
    "created_at": { "type": "string", "format": "date-time" },
    "title": { "$ref": "#/$defs/nonEmptyString" },
    "summary": { "$ref": "#/$defs/nonEmptyString" },
    "provenance": { "$ref": "#/$defs/provenance" },
    "scope": { "$ref": "#/$defs/scope" },
    "evidence_requirements": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/evidenceRequirement" }
    },
    "predicates": {
      "type": "object",
      "minProperties": 1,
      "propertyNames": { "$ref": "#/$defs/id" },
      "additionalProperties": { "$ref": "#/$defs/predicate" }
    },
    "gates": {
      "type": "object",
      "required": ["watch", "act", "pause", "reverse", "recover", "graduate"],
      "properties": {
        "watch": { "$ref": "#/$defs/expression" },
        "act": { "$ref": "#/$defs/expression" },
        "pause": { "$ref": "#/$defs/expression" },
        "reverse": { "$ref": "#/$defs/expression" },
        "recover": { "$ref": "#/$defs/expression" },
        "graduate": { "$ref": "#/$defs/expression" }
      },
      "additionalProperties": false
    },
    "governance": { "$ref": "#/$defs/governance" }
  },
  "additionalProperties": false,
  "allOf": [
    {
      "if": {
        "properties": {
          "governance": {
            "type": "object",
            "properties": {
              "lifecycle": { "enum": ["approved", "active", "paused"] }
            },
            "required": ["lifecycle"]
          }
        },
        "required": ["governance"]
      },
      "then": {
        "properties": {
          "governance": {
            "type": "object",
            "properties": {
              "approved_by": { "type": "array", "minItems": 1 }
            }
          }
        }
      }
    }
  ],
  "$defs": {
    "nonEmptyString": { "type": "string", "minLength": 1 },
    "id": { "type": "string", "pattern": "^[a-z0-9]+(?:[._-][a-z0-9]+)*$" },
    "provenance": {
      "type": "object",
      "required": ["classification", "authors", "rationale"],
      "properties": {
        "classification": { "enum": ["captured", "research-derived", "commissioned-proposal"] },
        "authors": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/nonEmptyString" }
        },
        "rationale": { "$ref": "#/$defs/nonEmptyString" },
        "source_capture_ids": {
          "type": "array",
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/id" }
        }
      },
      "additionalProperties": false
    },
    "evidenceRequirement": {
      "type": "object",
      "required": ["id", "description"],
      "properties": {
        "id": { "$ref": "#/$defs/id" },
        "description": { "$ref": "#/$defs/nonEmptyString" }
      },
      "additionalProperties": false
    },
    "scope": {
      "type": "object",
      "required": ["jurisdictions", "geographies", "cohorts", "services"],
      "properties": {
        "jurisdictions": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/nonEmptyString" }
        },
        "geographies": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/nonEmptyString" }
        },
        "cohorts": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/nonEmptyString" }
        },
        "services": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/nonEmptyString" }
        }
      },
      "additionalProperties": false
    },
    "expression": {
      "oneOf": [
        {
          "type": "object",
          "required": ["predicate_ref"],
          "properties": { "predicate_ref": { "$ref": "#/$defs/id" } },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": ["all"],
          "properties": {
            "all": {
              "type": "array",
              "minItems": 2,
              "items": { "$ref": "#/$defs/expression" }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": ["any"],
          "properties": {
            "any": {
              "type": "array",
              "minItems": 2,
              "items": { "$ref": "#/$defs/expression" }
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": ["not"],
          "properties": { "not": { "$ref": "#/$defs/expression" } },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": ["alternative_if"],
          "properties": {
            "alternative_if": {
              "type": "object",
              "description": "An authorised equivalent route. The expression is condition OR alternative.",
              "required": ["condition", "alternative"],
              "properties": {
                "condition": { "$ref": "#/$defs/expression" },
                "alternative": { "$ref": "#/$defs/expression" }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": ["veto_if"],
          "properties": {
            "veto_if": {
              "type": "object",
              "description": "A safety blocker. The expression is condition AND NOT blocker.",
              "required": ["condition", "blocker"],
              "properties": {
                "condition": { "$ref": "#/$defs/expression" },
                "blocker": { "$ref": "#/$defs/expression" }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "predicate": {
      "type": "object",
      "required": ["signal_id", "operator", "threshold", "window", "evidence"],
      "properties": {
        "signal_id": { "$ref": "#/$defs/id" },
        "operator": {
          "enum": [
            "gt",
            "gte",
            "lt",
            "lte",
            "eq",
            "neq",
            "change_gt",
            "change_gte",
            "change_lt",
            "change_lte"
          ]
        },
        "threshold": {
          "type": "object",
          "required": ["value", "unit", "rationale"],
          "properties": {
            "value": { "type": "number" },
            "unit": { "$ref": "#/$defs/nonEmptyString" },
            "rationale": { "$ref": "#/$defs/nonEmptyString" }
          },
          "additionalProperties": false
        },
        "window": {
          "type": "object",
          "required": ["lookback", "unit", "minimum_observations", "persistence"],
          "properties": {
            "lookback": { "type": "integer", "minimum": 1 },
            "unit": { "enum": ["day", "week", "month", "quarter", "year"] },
            "minimum_observations": { "type": "integer", "minimum": 1 },
            "persistence": { "type": "integer", "minimum": 1 },
            "maximum_age": { "type": "integer", "minimum": 1 }
          },
          "additionalProperties": false
        },
        "evidence": {
          "type": "object",
          "required": [
            "source_ids",
            "minimum_sources",
            "minimum_quality",
            "require_uncertainty"
          ],
          "properties": {
            "source_ids": {
              "type": "array",
              "minItems": 1,
              "uniqueItems": true,
              "items": { "$ref": "#/$defs/id" }
            },
            "minimum_sources": { "type": "integer", "minimum": 1 },
            "minimum_quality": { "enum": ["exploratory", "provisional", "validated"] },
            "require_uncertainty": { "type": "boolean" },
            "minimum_probability": { "type": "number", "minimum": 0, "maximum": 1 }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "owner": {
      "type": "object",
      "required": ["organisation", "role"],
      "properties": {
        "organisation": { "$ref": "#/$defs/nonEmptyString" },
        "role": { "$ref": "#/$defs/nonEmptyString" }
      },
      "additionalProperties": false
    },
    "approval": {
      "type": "object",
      "required": ["organisation", "role", "approved_at"],
      "properties": {
        "organisation": { "$ref": "#/$defs/nonEmptyString" },
        "role": { "$ref": "#/$defs/nonEmptyString" },
        "approved_at": { "type": "string", "format": "date-time" }
      },
      "additionalProperties": false
    },
    "governance": {
      "type": "object",
      "required": [
        "owner",
        "lifecycle",
        "approved_by",
        "valid_from",
        "expires_at",
        "review_every"
      ],
      "properties": {
        "owner": { "$ref": "#/$defs/owner" },
        "lifecycle": { "enum": ["draft", "shadow", "approved", "active", "paused", "retired"] },
        "approved_by": {
          "type": "array",
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/approval" }
        },
        "valid_from": { "type": "string", "format": "date-time" },
        "expires_at": { "type": "string", "format": "date-time" },
        "review_every": {
          "type": "object",
          "required": ["value", "unit"],
          "properties": {
            "value": { "type": "integer", "minimum": 1 },
            "unit": { "enum": ["day", "week", "month", "quarter", "year"] }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    }
  }
}
